using UnityEngine;

public class MissionRobotDialoguePresenter : MonoBehaviour
{
    // Mission intro dialogue is now owned by the Game Flow System.
    // This component is kept only so existing scene references do not become missing scripts.
}
